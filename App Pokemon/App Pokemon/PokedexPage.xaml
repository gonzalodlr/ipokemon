<Page
    x:Class="App_Pokemon.PokedexPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Windows.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:my="using:Dracofire"
    xmlns:my1="using:CharizardASM"
    xmlns:my2="using:LucarioGAC"
    xmlns:my3="using:Pokemon_Antonio_Campallo_Gomez"
    xmlns:my4="using:Sesion4"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.Background>
            <ImageBrush Opacity="0.5" ImageSource="/Assets/imgPokedex/pokedexBackground.jpg" Stretch="UniformToFill"/>
        </Grid.Background>

        <GridView   x:Name="ContentGridView" Margin="10"
                    IsItemClickEnabled="False"
                    CanDragItems="True"
                    AllowDrop="True"
                    CanReorderItems="True"
                    SelectionMode="Single"
                    SelectionChanged="ContentGridView_SelectionChanged"
                    ItemClick="ContentGridView_ItemClick"
                    FlowDirection="LeftToRight">

            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapGrid Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>

            <!-- Aquí se inserta el DataTemplate -->
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid AutomationProperties.Name="{Binding Nombre}" Width="880">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!-- Image can be replaced with custom icon for the type -->
                        <ContentPresenter Content="{Binding}" Width="500" Height="500"/>
                        <StackPanel Grid.Column="1" Margin="8,0,0,8">
                            <TextBlock Text="{Binding Nombre}" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Categoría}" Style="{ThemeResource CaptionTextBlockStyle}"/>
                                <TextBlock Text=" - " Style="{ThemeResource CaptionTextBlockStyle}"/>
                                <TextBlock Text="{Binding Tipo}" Style="{ThemeResource CaptionTextBlockStyle}"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>

            <!-- Los controles de usuario -->
            <my:DracofireGDLRS x:Name="DracofireGDLRS" Margin="5"/>
            <my4:GengarJCC x:Name="GengarJCC" Margin="5"/>
            
            <my2:MyUCLucario x:Name="MyUCLucario" Margin="5"/>
            <my3:ArticunoACG x:Name="ArticunoACG" Margin="5"/>
            
        </GridView>
    </Grid>
</Page>
